pipeline {
agent  {
label 'DevServer'
}

  stages{
    stage ('Build'){
    steps {
    echo "this is build stage"
    sh 'mvn clean package'
    }

post{
success{
archiveArtifacts artifacts: '**/target/*.war'
}
}
   }

  
  }
}
